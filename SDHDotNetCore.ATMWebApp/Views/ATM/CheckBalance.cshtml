@model ATMUserModel
@{
    ViewData["Title"] = "Check Balance";
}

<div class="container">
    <h2>Check Balance</h2>

    <form method="get" action="/ATM/CheckBalance/@Model.UserId">
        @if (ViewData["Balance"] != null)
        {
            <p>Your current balance: $@ViewData["Balance"]</p>
        }
        else
        {
            <p>Unable to retrieve balance.</p>
        }

        <a class="btn btn-warning" asp-controller="ATM" asp-action="CheckBalance" method="get">Refresh Balance</a>
        <a class="btn btn-info" href="/ATM/MainMenu/@Model.UserId">Back to Main Menu</a>
    </form>

</div>





